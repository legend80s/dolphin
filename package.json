{
  "name": "dolphin",
  "version": "1.0.0",
  "description": "🐬 An AutoTextDecoder Chrome Extension. Apply `decodeURIComponent` to all the encoded content in the page.",
  "main": "background.js",
  "scripts": {
    "mkdir-dist": "rm -rf dist &&  mkdir dist",
    "build": "npm run mkdir-dist && crx pack ./src -o dist/dolphin.crx",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "Chrome",
    "Extension",
    "Decode",
    "decodeURIComponent"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "crx": "^5.0.1",
    "git-commit-msg-linter": "^2.3.9"
  },
  "publishConfig": { "registry": "https://npm.pkg.github.com/" },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/legend80s/dolphin.git"
  },
  "bugs": {
    "url": "https://github.com/legend80s/dolphin/issues"
  },
  "homepage": "https://github.com/legend80s/dolphin#readme"
}
